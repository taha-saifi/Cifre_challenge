# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Java / Maven (vendored MinIE build artifacts and local repo cache)
extraction_pipeline/.m2/
extraction_pipeline/vendor/minie/target/
extraction_pipeline/vendor/minie/**/*.class

# Pipeline run logs (regenerated on every run, not source)
extraction_pipeline/logs/

# Claude Code local, per-machine settings (not shared config)
.claude/settings.local.json

# Personal application material (not repo content)
files/Lettre_Motivation.md

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local secrets (OpenRouter API key) - never commit
.env

# Demo server: embedding cache and runtime log (regenerable, not source)
demo_server/rag_index.json
demo_server/server.log
demo_server/__pycache__/

# Live ingestion workspace: regenerated per run, holds third-party scraped text
live_corpus/
live_kg/
demo_server/live_test_run.json

# Per-corpus RAG embedding caches: large, regenerated by demo_server/rag.py
demo_server/rag_index_*.json
demo_server/live_compare_run.json
demo_server/test_run.json
